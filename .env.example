# Anthropic API key used exclusively by the enrichment/scoring pipeline.
# Copy this file to .env and fill in your key. .env is git-ignored.
ANTHROPIC_API_KEY=

# Claude model to use (default: claude-sonnet-4-6)
# CLAUDE_MODEL=claude-sonnet-4-6

# API secret for the triage POST endpoint.
# Leave blank for local dev (unauthenticated).
# Set to a long random string in production: openssl rand -base64 32
TRIAGE_API_SECRET=
NEXT_PUBLIC_TRIAGE_API_SECRET=
