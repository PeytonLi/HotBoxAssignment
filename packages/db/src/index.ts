// Placeholder. The Persistence/DB agent builds the Prisma + SQLite data layer here
// (lead records + triage state + timestamps) and an API/Prisma-backed TriageStore.
// See docs/agents/03-persistence-db.md.
export const DB_PLACEHOLDER = true;
