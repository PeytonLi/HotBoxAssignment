// Placeholder. The Pipeline agent builds the enrich -> score pipeline here:
// load data/*.json -> enrich (tool-use) -> score+summary (tool-use) -> write results.json,
// with content-hash cache, p-limit concurrency, per-lead isolation + retries.
// See docs/agents/01-pipeline.md.
console.log("[pipeline] not implemented yet — see docs/agents/01-pipeline.md");
